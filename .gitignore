# Allowlist: only these belong in the repo. Everything else (uploads, drafts,
# exports, OS cruft) stays out.
*
!index.html
!v2.html
!family.js
!README.md
!.gitignore
# family photos for the tree (when added)
!img/
!img/**
